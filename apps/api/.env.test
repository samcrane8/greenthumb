# Test environment. Isolates persistence so functional tests never touch the
# real (git-tracked) model store. `tmp/` is gitignored.
TZ=UTC
PORT=3334
HOST=localhost
LOG_LEVEL=silent
NODE_ENV=test
MODELS_DIR=./tmp/test-models
DB_PATH=./tmp/test.sqlite
PROVIDERS_CONFIG_PATH=./tmp/test-providers.json
