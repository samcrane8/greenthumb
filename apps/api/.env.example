TZ=UTC
PORT=3333
HOST=localhost
LOG_LEVEL=info
NODE_ENV=development
APP_KEY=

# Storage & persistence (optional; sensible defaults under ./storage)
# DB_PATH=./storage/greenthumb.sqlite
# MODELS_DIR=./storage/models

# Single-tenant cloud gate. Unset = local mode (open, no egress).
# API_KEY=change-me-in-cloud
