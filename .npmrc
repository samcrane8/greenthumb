# Hoist nothing by default keeps the tree strict; loosen for tools that expect
# a flat node_modules (electron-builder, adonis ace peer resolution).
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
